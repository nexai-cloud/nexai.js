import { ReactNode } from "react";
type Props = {
    children: ReactNode;
    tooltip: ReactNode;
    className?: string;
};
export declare const TooltipWrap: ({ children, tooltip, className }: Props) => import("react/jsx-runtime").JSX.Element;
export {};
