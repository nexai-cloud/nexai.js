export declare const sendAiChat: ({ nexaiApiUrl, message, sessionId, projectId, name, }: {
    nexaiApiUrl: string;
    message: string;
    sessionId: string;
    projectId: string;
    name: string;
}) => Promise<any>;
