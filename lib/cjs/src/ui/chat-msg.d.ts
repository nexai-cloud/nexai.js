import { ChatMessage, type ChatThread } from "../chat-types";
export declare const NexaiChatMsg: (({ thread, chatMessage }: {
    thread: ChatThread;
    chatMessage: ChatMessage;
}) => import("react/jsx-runtime").JSX.Element | null) & {
    displayName: string;
};
